flask>=2.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0

# Fuzzy search (strongly recommended)
rapidfuzz>=3.0.0

# Chinese pinyin search (recommended for CN market)
pypinyin>=0.48.0

# Content extraction — the core differentiator vs Spotlight/Everything
pypdf>=3.0.0          # PDF text extraction
python-docx>=0.8.11   # Word document text extraction
openpyxl>=3.1.0       # Excel sheet name + cell extraction
